In order to complete this assignment I decided to use an algorithm
called BFS(Breadth First Search) which gives the nodes at every level and adding
them to the list before going down to the next layer.